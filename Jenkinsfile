pipeline {
 agent any 
 stages {
  stage("cheking") {
   steps {
    echo "cheking for code.."
   }
  }
  stage("testing") {
   steps {
    echo "testing for code"
    }
   }
  stage("deploying") {
   steps {
    echo "deploying for code"
   }
 }
}
}
